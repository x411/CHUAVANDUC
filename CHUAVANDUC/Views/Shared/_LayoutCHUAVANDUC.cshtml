<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@System.Configuration.ConfigurationManager.AppSettings["SiteName"]</title>

    <link href="~/Content/devoops/css/font-awesome.min.css" rel="stylesheet" />

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Content/animate.css" rel="stylesheet" />
    <link href="~/Content/devoops/plugins/camera/camera.css" rel="stylesheet" />
    <link href="~/Content/devoops/plugins/camera/camera.css" rel="stylesheet" />
    <link href="~/Content/devoops/plugins/slick/slick.css" rel="stylesheet" />
    <link href="~/Content/devoops/plugins/jis/stylesheets/jis.min.css" rel="stylesheet" />
    @RenderSection("styles", required: false)
</head>
<body>
    <div id="modalbox">
        <div class="devoops-modal">
            <div class="devoops-modal-header">
                <div class="modal-header-name">
                    <span>Basic table</span>
                </div>
                <div class="box-icons">
                    <a class="close-link">
                        <i class="fa fa-times"></i>
                    </a>
                </div>
            </div>
            <div class="devoops-modal-inner">
            </div>
            <div class="devoops-modal-bottom">
            </div>
        </div>
    </div>
    <div class="container">
        @{Html.RenderA ction("header");}

        <!--body content-->
        <div class="row">
            <!--cols left-->
            <div class="col-xs-12 col-sm-12 col-md-9" id="ajax-content">
                @RenderBody()
            </div>
            <!--end cols left-->
            <!--cols right-->
            <div class="col-xs-0 col-sm-0 col-md-3 col-right p_left_0">
                @{ Html.RenderAction("ColsRightBody", "Home", new { area = "" });}
            </div>
            <!--end cols right-->
        </div>
        <!--end body content-->

        <footer class="row">
            <div class="col-xs-12">
                <div class="company">
                    <span class="color_green">THỰC HIỆN: BAN TRUYỀN THÔNG CLB THIỆN NGUYỆN NẾP SỐNG ĐẠO</span>
                    <p class="color_green">Chịu trách nhiệm nội dung: Tổ Biên Tập-Truyền Thông</p>
                    <p class="color_green">CLB Thiện Nguyện NẾP SỐNG ĐẠO-Những Trái Tim Làm Đẹp Cuộc Đời</p>
                    <p class="color_green">Điện thoại: Tịnh Hướng: 0907.762.360 - Bảo Ngọc: 0909.969.477</p>
                    <p class="color_green">Mọi ý kiến đóng góp hay gửi bài, vui lòng liên hệ Email:<a href="mailto:nepsongdao1010@gmail.com">nepsongdao1010@gmail.com</a></p>
                    <br>
                    <p style="text-align: center; color: gray; font-size: 12px;">&copy; Copyright &#64; 2016 CLB Nếp Sống Đạo - All rights reserved</p>
                </div>
                <div class="clr"></div>
            </div>
        </footer>
    </div>
    <script src="~/Scripts/jquery-3.1.1.js"></script>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    <script src="~/Content/devoops/plugins/camera/camera.js"></script>
    <script src="~/Content/devoops/plugins/camera/jquery.easing.1.3.js"></script>
    @*<script src="~/Content/devoops/plugins/slick/slick.js"></script>*@
    <script src="~/Content/devoops/plugins/slick/slick.min.js"></script>
    <script src="~/Content/devoops/plugins/jis/javascript/jis.jquery.js"></script>
    <script src="~/Content/devoops/js/devoops.js"></script>
    <div id="fb-root"></div>
    <script>
        (function (d, s, id) {
            var js, fjs = d.getElementsByTagName(s)[0];
            if (d.getElementById(id)) return;
            js = d.createElement(s); js.id = id;
            js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&version=v2.7&appId=1550512665247038";
            fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));
    </script>
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date(); a = s.createElement(o),
            m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-60142784-2', 'auto');
        ga('send', 'pageview');

    </script>
    <script type="text/javascript">
        var sessionTimeoutWarning = @Session.Timeout -1;

        var sTimeout = parseInt(sessionTimeoutWarning) * 60 * 1000;
        setTimeout('SessionEnd()', sTimeout);

        wow = new WOW({
		    animateClass: 'animated',
		    offset: 50,
		    callback: function (box) {
		        console.log("WOW: animating <" + box.tagName.toLowerCase() + ">")
		    }
		});

        wow.init();

        //$('#camera_banner').camera({
        //    playPause: false,
        //    pauseOnClick: false,
        //    hover: false,
        //    navigation: false,
        //    height: '300',

        //});

        //$('#rev_slider').slick({
        //    arrows: false,
        //    dots: false,
        //    fade: true,
        //    lazyLoad: 'ondemand',
        //});

        $('.demo').jis();

        $("a.submenu").click(function () {
            $(".menuBar").slideToggle("normal", function () {
                // Animation complete.
            });
        });

        $("nav .menuBar ul li a").click(function (e) {
            var self = $(this);
            if (!self.parent().hasClass('dropdown')) {
                //GET url
                var url = self.attr('href');
                var isAjax = $(this).attr('data-ajax');

                if ($('a.submenu').is(':visible')) {
                    $("ul.menu li").removeClass("active");

                    self.parent().addClass('active');

                    if (self.next('.menu').length || self.next('.sub_menu').length || self.next('.mega_menu').length) {
                        self.parent()
                            .addClass('current')
                            .siblings()
                            .removeClass('current')
                            .children(':not(a)')
                            .slideUp(300);
                        self.next().stop().slideToggle(300);
                        self.parent().siblings().children('a').removeClass('prevented');

                        if (!(self.hasClass('prevented'))) {
                            e.preventDefault();
                            self.addClass('prevented');
                        } else {
                            self.removeClass('prevented');
                        }
                    }
                }

                if (url != undefined && url != null && url != '' && url != '/') {
                    if (isAjax == 'True') {
                        LoadAjaxContent(url, '');
                        e.preventDefault();
                    }
                    else {

                    }
                }

            }
        });

    </script>

    @RenderSection("scripts", required: false)
</body>
</html>
